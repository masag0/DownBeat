import React from 'react';

const initalState = {};

const songsReducer = (oldState = initalState, action) => {
  switch (action.type) {
    // case expression:
    //
    //   break;
    default:
      return oldState;
  }
};

export default songsReducer;